## Mô tả



## Kiểm tra

- Đã chạy `yarn install --frozen-lockfile` (root) và build client (`cd client && yarn build`) nếu thay đổi phụ thuộc hoặc frontend
- Không commit secret / `.env` (chỉ cập nhật `config/default.json` với placeholder khi cần)

## Ghi chú

